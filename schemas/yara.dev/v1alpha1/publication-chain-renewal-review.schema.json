{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yara.dev/schemas/v1alpha1/publication-chain-renewal-review.schema.json",
  "title": "YARA PublicationChainRenewalReview v1alpha1",
  "description": "Immutable non-mutating publication-chain renewal review evidence for one assertion scope.",
  "type": "object",
  "additionalProperties": false,
  "required": ["apiVersion", "kind", "metadata", "spec"],
  "properties": {
    "apiVersion": { "const": "yara.dev/v1alpha1" },
    "kind": { "const": "PublicationChainRenewalReview" },
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "reviewId"],
      "properties": {
        "name": { "$ref": "#/$defs/name" },
        "reviewId": { "$ref": "#/$defs/digest" }
      }
    },
    "spec": {
      "type": "object",
      "additionalProperties": false,
      "required": ["reviewedAt", "expiresAt", "catalogDigest", "assertionRef", "publicationChainRehearsalId", "publicationChainRetentionAuditHead", "promotionReviewId", "lifecycleProofApprovalId", "integrationPublicationAttestationId", "selectedEvidence", "reviewer", "decision", "reasonReference", "maxEvidenceAge", "limitations"],
      "properties": {
        "reviewedAt": { "type": "string", "format": "date-time" },
        "expiresAt": { "type": "string", "format": "date-time" },
        "catalogDigest": { "$ref": "#/$defs/digest" },
        "assertionRef": { "type": "string", "minLength": 1 },
        "publicationChainRehearsalId": { "$ref": "#/$defs/digest" },
        "publicationChainRetentionAuditHead": { "$ref": "#/$defs/digest" },
        "promotionReviewId": { "$ref": "#/$defs/digest" },
        "lifecycleProofApprovalId": { "$ref": "#/$defs/digest" },
        "integrationPublicationAttestationId": { "$ref": "#/$defs/digest" },
        "selectedEvidence": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/digest" }
        },
        "reviewer": {
          "type": "object",
          "additionalProperties": false,
          "required": ["identity", "role", "assurance"],
          "properties": {
            "identity": { "type": "string", "minLength": 1 },
            "role": { "type": "string", "minLength": 1 },
            "assurance": { "type": "string", "minLength": 1 }
          }
        },
        "decision": { "enum": ["approved", "changes-required", "abstained"] },
        "reasonReference": { "type": "string", "minLength": 1 },
        "maxEvidenceAge": { "type": "string", "pattern": "^[0-9]+(ns|us|µs|ms|s|m|h)([0-9]+(ns|us|µs|ms|s|m|h))*$" },
        "limitations": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    }
  },
  "$defs": {
    "digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "name": { "type": "string", "pattern": "^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$" }
  }
}
