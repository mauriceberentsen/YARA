{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yara.dev/schemas/v1alpha1/acceptance-gate-review.schema.json",
  "title": "YARA AcceptanceGateReview v1alpha1",
  "type": "object",
  "additionalProperties": false,
  "required": ["apiVersion", "kind", "metadata", "spec"],
  "properties": {
    "apiVersion": { "const": "yara.dev/v1alpha1" },
    "kind": { "const": "AcceptanceGateReview" },
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "reviewId"],
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$" },
        "reviewId": { "$ref": "#/$defs/digest" }
      }
    },
    "spec": {
      "type": "object",
      "additionalProperties": false,
      "required": ["acceptanceCriterion", "reviewer", "reviewedAt", "verdict"],
      "properties": {
        "acceptanceCriterion": { "type": "integer", "enum": [2, 6, 8, 9, 11] },
        "reviewer": { "$ref": "#/$defs/reviewer" },
        "reviewedAt": { "type": "string", "minLength": 1 },
        "verdict": { "enum": ["approved", "changes-required", "abstained"] }
      }
    }
  },
  "$defs": {
    "digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
    "reviewer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["identity", "role", "assurance"],
      "properties": {
        "identity": { "type": "string", "minLength": 1 },
        "role": { "type": "string", "minLength": 1 },
        "assurance": { "type": "string", "minLength": 1 }
      }
    }
  }
}
